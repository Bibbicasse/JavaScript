// Le JS pour introduction comparaison 
let x = '4';

// console.log(x);
// console.log(typeof x)

let testEgalValeur = x == 4;

// console.log(testEgalValeur);
// console.log(typeof testEgalValeur);

let testEgaleValeurType = x === 4; // Condition remplis si valeur ET type identique.
// console.log(testEgaleValeurType);

let testDifferenceValeur = x != 4; //Différent de 4 ?
// console.log(testDifferenceValeur);

let testDifferentValeurType = x !== 4; //différent en valeur ET en type?
// console.log(testDifferentValeurType); 

let testInferieur = x < 4;
// console.log(testInferieur);

let testSuperieur = x > 4;
// console.log(testSuperieur);

let testInferieurOuEgale = x < 4;
// console.log(testInferieurOuEgale);

let testSuperieurOuEgale = x > 4;
// console.log(testSuperieurOuEgale);

// alert('Valeur dans x égale à 4 (en valeur) ? : ' + testEgalValeur +
// '\nValeur dans x égale à 4 (en valeur ET en type) ? ' + testEgaleValeurType +
// '\nValeur dans x différent de 4 (en valeur) ? ' + testDifferenceValeur +
// '\nValeur dans x différent de 4 (en valeur OU en type) ? ' + testDifferentValeurType +
// '\nValeur dans x inférieur à 4 (en valeur) ? ' + testInferieur + 
// '\nValeur dans x supérieur à 4 (en valeur) ? ' + testSuperieur + ' '
// );

// alert(`Valeur dans x égale à 4 (en valeur) ? : ${testEgalValeur}
// Valeur dans x égale à 4 (en valeur ET en type) ? ${testEgaleValeurType}
// Valeur dans x différent de 4 (en valeur) ? ${testDifferenceValeur}
// Valeur dans x différent de 4 (en valeur OU en type) ? ${testDifferentValeurType}
// Valeur dans x inférieur à 4 (en valeur) ? ${testInferieur}
// Valeur dans x supérieur à 4 (en valeur) ? ${testSuperieur} `
// );